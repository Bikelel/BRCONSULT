<odoo>
    <data>
        <record id="view_partner_form_br_consult" model="ir.ui.view">
            <field name="name">res.partner.form.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='contact_addresses']" position="attributes">
                    <!--TODO: Result from 'attrs' -> 'invisible' conversion without also overriding 'states' attribute
                         Check if this page tag contained a states attribute in any of the parent views, in which case it should be combined into this 'invisible' attribute
                         (If any states attributes existed in parent views, they'll also be marked with a TODO)-->
                    <attribute name="invisible">company_type != 'company'</attribute>
                </xpath>
                <xpath expr="//page[@name='sales_purchases']" position="attributes">
                    <attribute name="invisible">1</attribute>
                </xpath>
                <xpath expr="//field[@name='ref']" position="attributes">
                    <attribute name="required">company_type == 'company' and not is_mentor</attribute>
                    <!--TODO: Result from converting 'attrs' attribute override without options for ['invisible', 'readonly', 'column_invisible'] to separate attributes
                         Remove redundant empty tags below for any of those attributes that are not present in the field tag in any of the parent views
                         If someone later adds one of these attributes in the parent views, they would likely be unaware it's still overridden in this view, resulting in unexpected behaviour, which should be avoided-->
                    <!--TODO: Result from 'attrs' -> 'invisible' conversion without also overriding 'states' attribute
                         Check if this field tag contained a states attribute in any of the parent views, that should be combined into this 'invisible' attribute
                         (If any states attributes existed in parent views, they'll also be marked with a TODO)-->
                    <attribute name="invisible"/>
                    <attribute name="readonly"/>
                    <attribute name="column_invisible"/>
                </xpath>
                <xpath expr="//field[@name='vat']" position="before">
                    <field name="siret"/>
                    <xpath expr="//field[@name='ref']" position="move"/>
                    <xpath expr="//field[@name='industry_id']" position="move"/>
                </xpath>
                <xpath expr="//page[@name='internal_notes']" position="before">
                    <page name="legal_representative" string="Infos du représentant légal" invisible="is_mentor">
                        <group>
                            <group>
                                <field name="lastname_legal_representative"/>
                                <field name="firstname_legal_representative"/>
                                <field name="email_legal_representative"/>
                            </group>
                            <group>
                                <field name="phone_legal_representative"/>
                                <field name="mobil_phone_legal_representative"/>
                            </group>
                        </group>
                        
                    </page>
                </xpath>
                <xpath expr="//field[@name='category_id']" position="after">
                    <field name="is_mentor"/>
                </xpath>
                
                
            </field>
        </record>
    </data>
</odoo>